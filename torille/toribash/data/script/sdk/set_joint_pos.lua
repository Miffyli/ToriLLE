-- set_joint_pos (integer player_index, integer joint_index, number x, number y, number z)

-- USE: Set the position of a specified joint
-- NOTES: -

local player_index, joint_index, x, y, z = 0, 0, 0, 0, 0

echo ("set_joint_pos (" .. player_index .. ", " .. joint_index .. ", " .. x .. ", " .. y .. ", " .. z .. ")")
set_joint_pos (player_index, joint_index, x, y, z)
