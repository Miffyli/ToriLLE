-- head_info = get_head_info(number player_index)

-- USE: Returns an array containing head information
-- NOTES: -

local player_index = 0
local head_info = get_head_info(player_index)

echo (head_info .. " = get_head_info(" .. player_index .. ")")
--for key,value in pairs(head_info) do 
  -- echo (key ..  ": " .. value)
--end
