--set_body_texture(int body_part, int number(where number is the id after the name e.g. head0.tga(only valid for textures in data/script/customise))

-- USE: Set the body texture
-- NOTES: -

echo ("Texturing the HEAD")
set_body_texture(0,0)
