-- set_body_pos(number player_index, number body_index, number x, number y, number z)

-- USE: Sets the position of a body part
-- NOTES: -

local player_index, body_index, x, y, z = 0, 0, 0, 0, 0
set_body_pos(player_index, body_index, x, y, z)

echo("set_body_pos(" .. player_index .. ", " .. body_index .. ", " .. x .. ", " .. y .. ", " .. z .. ")")
